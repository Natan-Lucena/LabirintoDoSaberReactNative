// Destinos placeholder (docs/contracts/t-402-guarda-sessao.md): não existem
// ainda rotas reais de login (T-401) nem de área autenticada (T-502/T-601+).
// Ponto único de troca quando essas rotas existirem.
export const AUTH_DESTINATION = "/" as const;
export const APP_DESTINATION = "/" as const;
