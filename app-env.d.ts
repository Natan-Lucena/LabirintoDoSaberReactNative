/// <reference types="expo/types" />

// Versionado: garante as declarações do Expo (ex.: import de *.css) no typecheck
// sem depender do expo-env.d.ts, que o `expo start` gera e ignora no git
// (docs/bootstrap/COMPATIBILIDADE.md, A-05).
