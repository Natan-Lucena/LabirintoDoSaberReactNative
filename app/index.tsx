import { Text, View } from "react-native";

// Rota inicial provisória; substituída pelo fluxo de Login/Home (T-401, T-603).
export default function Index() {
  return (
    <View className="flex-1 items-center justify-center bg-white">
      <Text className="text-xl font-bold">Labirinto do Saber</Text>
    </View>
  );
}
